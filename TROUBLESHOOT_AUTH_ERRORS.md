# 🔍 استكشاف أخطاء التسجيل والدخول

## 🚨 الخطأ: "Registration error: {}"

هذا الخطأ يعني أن الـ response من السيرفر فارغ أو لم يصل الطلب أصلاً.

---

## ✅ خطوات الحل

### 1️⃣ تحقق من البياك إند

```bash
# هل البياك إند مشغل؟
http://192.168.1.158:5000

# إذا لم يكن مشغلاً، شغّله الآن!
```

### 2️⃣ افتح Console في المتصفح (F12)

ستشاهد رسائل مفصلة:

```javascript
📤 جاري الإرسال: { name: "...", email: "...", phone: "..." }

❌ Registration error details: {
  message: "...",
  status: 0 || 500 || 400,
  data: { message: "..." },
  url: "/users/register"
}
```

### 3️⃣ اقرأ الرسالة الدقيقة

#### إذا كان status = 0 أو "Network Error":
```
❌ المشكلة: لا يمكن الوصول للسيرفر
✅ الحل: تأكد من:
   1. البياك إند مشغل على 192.168.1.158:5000
   2. الشبكة متصلة
   3. الفايروول لا يحظر الاتصال
```

#### إذا كان status = 404:
```
❌ المشكلة: Endpoint /users/register غير موجود
✅ الحل: 
   1. البياك إند قد لا يحتوي على هذا المسار
   2. قد يكون المسار مختلف (/api/auth/register)
   3. تحقق من routes في Backend
```

#### إذا كان status = 400:
```
❌ المشكلة: البيانات المرسلة غير صحيحة
✅ الحل:
   1. تحقق من اسم الحقول (name, email, phone, password)
   2. تأكد من صحة البريد والهاتف
   3. كلمة المرور يجب أن تحقق الشروط
```

#### إذا كان status = 500:
```
❌ المشكلة: خطأ في السيرفر
✅ الحل:
   1. شغّل الـ backend مرة أخرى
   2. تحقق من logs في Backend
   3. قد تكون قاعدة البيانات معطلة
```

#### إذا كان status = 409:
```
❌ المشكلة: البريد الإلكتروني مسجل بالفعل
✅ الحل:
   1. استخدم بريد إلكتروني مختلف
   2. أو سجل دخول بحساب موجود
```

---

## 🧪 اختبار الاتصال بالسيرفر

### الطريقة 1: في المتصفح

```javascript
// افتح Console (F12) واكتب:
fetch('http://192.168.1.158:5000/api/health')
  .then(r => r.json())
  .then(d => console.log('✅ السيرفر يرد:', d))
  .catch(e => console.error('❌ لا يرد السيرفر:', e.message))
```

### الطريقة 2: باستخدام curl (في Terminal)

```bash
curl http://192.168.1.158:5000/api/health
```

### الطريقة 3: اختبار التسجيل مباشرة

```bash
curl -X POST http://192.168.1.158:5000/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "phone": "9661234567",
    "password": "Test@123456"
  }'
```

---

## 📊 جدول الأخطاء الشاملة

| الخطأ | السبب | الحل |
|-------|-------|-----|
| status = 0 | لا يرد السيرفر | شغّل Backend |
| status = 404 | Endpoint غير موجود | تحقق من المسار |
| status = 400 | بيانات غير صحيحة | تحقق من البيانات |
| status = 409 | البريد مسجل | استخدم بريد آخر |
| status = 500 | خطأ السيرفر | أعد تشغيل Backend |
| "Network Error" | مشكلة في الشبكة | تحقق من الاتصال |

---

## 🔧 الملفات التي تم تحديثها

- ✅ `app/register/page.js` - رسائل خطأ أفضل
- ✅ `app/login/page.js` - رسائل خطأ أفضل

---

## 📝 معلومات مهمة

### البيانات المطلوبة للتسجيل:
```javascript
{
  name: string (إلزامي),
  email: string (إلزامي، فريد),
  phone: string (إلزامي),
  password: string (إلزامي، 8+ أحرف، شروط)
}
```

### الشروط التي يجب أن يفحصها Backend:
- ✅ البريد يجب أن يكون فريداً
- ✅ البريد صيغته صحيحة
- ✅ كلمة المرور قوية (8+ أحرف)
- ✅ الهاتف صيغته صحيحة

---

## 🎯 الخطوات التالية

1. **افتح Console (F12)**
2. **جرّب التسجيل مرة أخرى**
3. **اقرأ الرسالة التفصيلية**
4. **طبّق الحل المناسب**
5. **جرّب مرة أخرى**

---

## 💡 نصيحة سريعة

إذا كنت تواجه مشكلة:

```javascript
// في Console، اكتب:
window.authDebug.showTokenInfo()

// أو اختبر الـ axios:
fetch('http://192.168.1.158:5000/api/health')
  .then(r => console.log('✅ يرد السيرفر'))
  .catch(e => console.error('❌', e.message))
```

---

**هل تزال تواجه مشكلة؟ شارك معي رسالة الخطأ التفصيلية من Console!** 🔧
