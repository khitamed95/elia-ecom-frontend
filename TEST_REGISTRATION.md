# 📋 خطوات الاختبار الناجحة

## ✅ ما تم تصحيحه

الملف `app/register/page.js` تم تحديثه ليرسل الطلب للمسار الصحيح:
```
من: POST /users
إلى: POST /users/register ✅
```

---

## 🧪 الاختبار الآن

### الخطوة 1: افتح صفحة التسجيل
```
http://localhost:3000/register
```

### الخطوة 2: أدخل البيانات التالية

```
✍️ الاسم الكامل:
   Admin User

📧 البريد الإلكتروني:
   admin@elia-store.com

📱 رقم الهاتف:
   966 (اختر السعودية) + أي رقم
   أو 970 (اختر العراق) + أي رقم

🔐 كلمة المرور:
   Admin@123456

   ❗ يجب أن تحتوي على:
   - 8 أحرف على الأقل ✅
   - حرف كبير (A-Z) ✅
   - حرف صغير (a-z) ✅
   - رقم (0-9) ✅
   - رمز خاص (!@#$) ✅
```

### الخطوة 3: اضغط زر "إنشاء الحساب"

### الخطوة 4: انتظر النتيجة

#### إذا نجح:
```javascript
// سترى:
✅ رسالة نجاح
📍 توجيه تلقائي للصفحة الرئيسية
localStorage سيحتوي على userInfo
```

#### إذا فشل:
```javascript
// ستشاهد الخطأ في:
1. رسالة toast (أسفل الشاشة)
2. Console (F12)
3. Network Tab (طلب API)
```

---

## 🔍 التحقق من النجاح

### في المتصفح - افتح Console (F12):
```javascript
// اكتب:
JSON.parse(localStorage.getItem('userInfo'))

// يجب أن تشاهد:
{
  id: "abc123",
  email: "admin@elia-store.com",
  name: "Admin User",
  phone: "966...",
  accessToken: "eyJ...",
  refreshToken: "eyJ..." // قد يكون موجود أو لا
}
```

### في Network Tab:
```
Request:  POST /users/register
Status:   201 Created ✅ أو 200 OK ✅
Response: كائن المستخدم
```

---

## ⚠️ الأخطاء المتوقعة

### ❌ "Cannot POST /users/register"
**السبب:** Backend endpoint غير موجود
**الحل:** تأكد من وجود `/users/register` في البياك إند

### ❌ "البريد الإلكتروني مسجل بالفعل"
**السبب:** الحساب موجود بالفعل
**الحل:** استخدم بريد إلكتروني آخر

### ❌ "خطأ في بيانات الدخول"
**السبب:** أحد الحقول فارغ أو خاطئ
**الحل:** تحقق من جميع البيانات

### ❌ "Error: ECONNREFUSED"
**السبب:** Backend غير مشغل
**الحل:** شغّل Backend على `192.168.1.158:5000`

---

## ✅ بعد التسجيل بنجاح

### 1. تسجيل الدخول
```
الرابط: http://localhost:3000/login
أدخل نفس البيانات (البريد + كلمة المرور)
```

### 2. فحص الجلسة
```javascript
// في Console:
window.authDebug.showTokenInfo()
```

### 3. الوصول للصفحات المحمية
```
http://localhost:3000/admin     (إذا كنت Admin)
http://localhost:3000/contact   (للتواصل)
http://localhost:3000/profile   (ملفك الشخصي)
```

---

## 📝 ملاحظات إضافية

1. **الهاتف:** تطبيق يدعم أرقام دولية
   - العراق: 970 أو 964
   - السعودية: 966
   - الإمارات: 971

2. **كلمة المرور:** قوية جداً لضمان الأمان

3. **البريد الإلكتروني:** يجب أن يكون فريداً

4. **الحساب الجديد:** سيكون عادياً (User) وليس Admin
   - لجعله Admin، عدّل في قاعدة البيانات

---

## 🚀 الخطوة التالية

بعد التسجيل الناجح:
1. سجل دخول مرة أخرى من `/login`
2. اختبر الصفحات المحمية
3. جرب نموذج التواصل
4. تحقق من حفظ البيانات بشكل صحيح

---

**جاهز للاختبار؟** اضغط زر التسجيل الآن! 🚀
